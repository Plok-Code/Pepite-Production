[mysql]
# host = "127.0.0.1"
# port = 3306
# user = "root"
# password = "CHANGE_ME"
# database = "wildflix"

[powerbi]
# SIMPLE_URL = "https://app.powerbi.com/reportEmbed?reportId=...&autoAuth=true&ctid=..."
# Alternative (deployments): set env var `WILDFLIX_POWERBI_SIMPLE_URL`.

# Used to sign auth tokens (recommended in production).
# SECRET_KEY = "CHANGE_ME"

[auth]
# Accounts listed here can use the app like admins but cannot change login email nor password.
protected_accounts=["shared.admin@wildflix.com"]
